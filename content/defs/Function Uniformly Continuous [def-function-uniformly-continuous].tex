% defined-in: spivak (page 163), spivak (page 487), spivak (page 165)
% === SEMANTIC MAPPING ===
% 1. Variables & Types: 
%    - f : [a, b] → ℝ (Function)
%    - a, b : ℝ (Real numbers)
%    - ε, δ : ℝ (Real numbers)
%    - x, y : ℝ (Real numbers)
%    - A : Set of ℝ (Subset of real numbers)
% 2. Data Structures: 
%    - Interval [a, b] represented as a subset of ℝ.
%    - Absolute value represented as \RealAbsoluteValue{\mathrm{abs}}.
% 3. Implicit Bounds: 
%    - ε > 0, δ > 0.
% ========================

% entity-id: def-function-uniformly-continuous
% entity-type: def
% macro: \FunctionUniformlyContinuous
% args: 1
% notation: #1

\hypertarget{def-function-uniformly-continuous}{}
\begin{definition}[def-function-uniformly-continuous]
\[
\mathrm{IsUniformlyContinuous}(f, A) \TermDefinedAs \TerForall \varepsilon \in \RealNumbers \left( \varepsilon > 0 \TermImplication \TermExists \delta \in \RealNumbers \left( \delta > 0 \TermConjunction \TerForall x \in A \TerForall y \in A \left( \RealAbsoluteValue{x - y} < \delta \TermImplication \RealAbsoluteValue{f(x) - f(y)} < \varepsilon \right) \right) \right)
\]
\end{definition}